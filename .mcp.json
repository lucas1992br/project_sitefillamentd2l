{
    "mcpServers": {
        "laravel-boost": {
            "command": "wsl.exe",
            "args": [
                "/home/lucas/.config/herd-lite/bin/php",
                "/home/lucas/d2l-app/artisan",
                "boost:mcp"
            ]
        },
        "stitch": {
            "serverUrl": "https://stitch.googleapis.com/mcp",
            "headers": {
                "X-Goog-Api-Key": "${GOOG_API_KEY}"
            }
        }
    }
}